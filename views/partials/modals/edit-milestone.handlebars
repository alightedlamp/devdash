<div class="modal edit-modal">
    <div class="modal-background"></div>
    <div class="modal-card">
        <header class="modal-card-head">
            <p class="modal-card-title">Edit</p>
        </header>
        <section class="modal-card-body">
            <!-- Milestone Title Input -->
            <div class="field">
                <label class="label">Title</label>
                <div class="control">
                    <input class="input" type="text" placeholder="" id="milestoneEditTitleField">
                </div>
            </div>

            <!-- Milestone Description Input -->
            <div class="field">
                <label class="label">Description</label>
                <div class="control">
                    <input class="input" type="text" placeholder="" id="milestoneEditDescriptionField">
                </div>
            </div>


            <!-- Milestone Target Completion Input -->
            <div class="field">
                <label class="label">Target Completion Date</label>
                <div class="control">
                    <input class="input" type="date" id="milestoneEditCompletionDateField">
                </div>
            </div>

            {{!-- <!-- Dropdown -->
            <div class="field">
                <label class="label">Priority</label>
                <div class="control">
                    <div class="select">
                        <select id="milestoneEditPriorityField">
                            <option value="high">High</option>
                            <option value="normal">Normal</option>
                            <option value="low">Low</option>
                        </select>
                    </div>
                </div>
            </div> --}}
        </section>

        <!-- Footer -->
        <footer class="modal-card-foot">
            <button class="button is-success" id="submitEditedMilestone" data-milestoneId="{{id}}">Save changes</button>
            <button class="button edit-cancel">Cancel</button>
        </footer>
    </div>
</div>